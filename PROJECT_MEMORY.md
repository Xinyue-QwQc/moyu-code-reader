# Project memory


## 20. 隐藏文本光标必须同时关闭所在行高亮（2026-09-14）

- 用户再次明确：选择“隐藏光标”后，不仅插入光标不可见，光标所在行也不能高亮，包括当前行正文背景、边框与行号区高亮。
- 点击正文、方向键移动、切章、切换焦点后仍须保持；不能只把光标颜色改透明便算完成。鼠标指针与用户主动选区高亮不得被隐藏。
- 必须用真实编辑器中的点击/移动和当前行样式验证，再交付产品。
